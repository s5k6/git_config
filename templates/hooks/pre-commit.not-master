#!/bin/bash
set -u -e -C
shopt -s nullglob

if git log -n0 &>/dev/null; then
    b="$(git symbolic-ref HEAD)"
    if test "$b" = "refs/heads/master"; then
        echo 'Thou shalt not commit to the master.'
        exit 1
    fi
fi
